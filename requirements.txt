# Flask Web 框架
Flask>=2.3.0

# MongoDB 驱动
pymongo>=4.5.0

# 日期时间处理
python-dateutil>=2.8.2

# CORS 支持（可选，用于前端开发调试）
flask-cors>=4.0.0

# HTTP 请求
requests>=2.31.0

# HTML 解析
beautifulsoup4>=4.12.0

# Crawl4AI（可选，用于动态页面爬取）
# crawl4ai>=0.2.0

# TLS 指纹伪装 HTTP 客户端（可选，用于绕过反爬）
# curl_cffi>=0.7.0

# 专业网页正文提取（可选，提升预览正文质量）
# trafilatura>=1.8.0

# 生产环境 WSGI 服务器（Windows 使用 waitress）
waitress>=2.1.0

# Telegram 客户端
telethon>=1.34.0

# 异步 HTTP（Webhook 推送）
aiohttp>=3.9.0
